<h6><%= @product.name %></h6>
<p><%= @product.price %></p>
<%= image_tag url_for(@product&.product_image), alt: "Test", class: 'post_img' %>
<%= link_to "Edit", edit_product_path(@product) %>
<%= button_to "Delete", product_path(@product), method: :delete %>
<%= link_to "Buy Now", make_payment_path(@product) %>
